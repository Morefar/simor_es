<%= form_for(@color) do |f| %>
  <%= render 'shared/error_messages', target: @color %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
