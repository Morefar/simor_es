<%= form_for(@build_restriction) do |f| %>
  <% if @build_restriction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build_restriction.errors.count, "error") %> prohibited this build_restriction from being saved:</h2>

      <ul>
      <% @build_restriction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kind_id %><br />
    <%= f.number_field :kind_id %>
  </div>
  <div class="field">
    <%= f.label :body_id %><br />
    <%= f.number_field :body_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
