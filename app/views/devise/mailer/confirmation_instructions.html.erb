<p><%= "#{t('.welcome')} #{@resource.first_name} #{@resource.last_name}" %>!</p>

<p><%= t('.content') %></p>

<p><%= link_to "#{t('.confirm_link')}", confirmation_url(@resource, :confirmation_token => @token) %></p>
