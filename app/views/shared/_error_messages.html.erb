<% if target.errors.any? %>
<div class="form-errors">
  <h2><%= pluralize(@contract.errors.count, "error") %> prohibited this record to be saved: </h2>
  <ul class='error-msgs'>
  <% target.errors.full_messages.each do |error_msg| %>
    <li><%= error_msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
